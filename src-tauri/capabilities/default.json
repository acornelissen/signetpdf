{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus clipboard write-text and window close. The dialog runs Rust-side inside the open_pdf command (not exposed to JS); the only plugin command on the JS surface is clipboard-manager write-text (context-menu Copy) and window close (re-issued after the unsaved-changes confirm). Clipboard read is deliberately not granted.",
  "windows": ["main"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text", "core:window:allow-close"]
}
