{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus clipboard write-text and window destroy. The dialog runs Rust-side inside the open_pdf command (not exposed to JS); the only plugin commands on the JS surface are clipboard-manager write-text (context-menu Copy) and window destroy (used by onCloseRequested to close after the unsaved-changes confirm). Clipboard read is deliberately not granted.",
  "windows": ["main"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text", "core:window:allow-destroy"]
}
